# 给 Gemini 的提示词：重写最终课程设计报告

你将收到一组课程设计材料。请基于这些材料，为《基于 PostgreSQL 的数字图像集成管理系统》重写一份可以直接整理成 DOCX 提交的《数据库系统课程设计报告》正文。

请先阅读我上传的教师任务书、课程设计报告模板、评分依据图片、重要答疑、需求分析正式版、概要设计、详细设计、README、schema.sql、代码片段、界面截图和当前报告草稿。材料优先级如下：

1. 教师任务书、课程设计报告模板、评分依据图片和重要答疑优先级最高。
2. `需求分析word正式版.doc` 是需求分析说明书的权威版本，优先使用它，不要用当前生成的需求分析草稿替代它。
3. README、schema.sql、pom.xml、关键 Java 代码、截图、验证日志用于判断当前系统真实实现。
4. 当前报告草稿只用于了解目前覆盖了哪些点，不要沿用它的写法、篇幅、句式和排版思路。

目标：

写出一份完整、扎实、自然的中文课程设计报告，正文至少 10000 个中文字符，建议控制在 10000 到 13000 字之间。不要写成 AI 味很重的短标签加冒号堆叠，也不要只罗列功能清单。报告要像学生认真完成了系统后写出来的正式报告，语言可以平实，但必须具体、有证据、有数据库含金量。

教师评分导向必须体现：

总成绩由文档、源代码、答辩和课程报告组成。PPT 同学评分依据中，数据库设计 30 分、功能设计 20 分、后台程序设计 15 分、界面设计 15 分、报告表述水平 10 分、PPT 表达能力 10 分。报告本身要明显服务于这些得分点，尤其要把数据库设计写透。

报告必须覆盖但不要机械照抄以下内容：

1. 引言、项目背景、系统目标、用户范围和系统边界。
2. 功能需求、系统范围、功能结构图或总用例图、总体流程、数据字典。
3. 数据库设计，包括实体来源、ER 关系、关系模式、外模式、物理结构、空值和约束取舍。
4. PostgreSQL 选择理由，结合 bytea、JSONB、递归 CTE、视图、触发器、存储过程、索引、事务、连接池说明。
5. schema.sql 中的真实数据库对象：13 张表，19 个索引（其中 18 个普通索引和 1 个唯一部分索引 `idx_images_hash`），4 个视图，4 个函数，5 个触发器，5 个存储过程。
6. 系统设计与实现，包括 JavaFX 桌面端、Controller/Service/DAO 分层、HikariCP、PreparedStatement、事务回滚、后台扫描、缩略图缓存、AI fallback、数据库初始化向导、离线降级。
7. 当前 README 中真实存在的功能：完整磁盘目录树懒加载、缩略图和元数据入库、bytea 缩略图、AI 标签、关键词搜索、NL2SQL 搜索、版本历史、幻灯片音乐、设置页、逐个供应商验证、数据库建立引导。
8. 测试和截图说明，引用我上传的界面截图文件名作为图的位置建议。
9. 安装和使用说明，区分 Java 环境下运行 JAR 与 Windows portable zip/exe。
10. 总结与不足，写清 WebDAV/云端、语音搜索、全网全盘搜索等只能作为预留或后续扩展，不能写成已完成主流程。

真实性边界：

不要夸大未完成内容。WebDAV/云端只写“表结构和依赖为后续扩展预留”。AI 功能需要用户自行配置 OpenAI-compatible endpoint 和密钥。存储过程可以作为数据库课程对象和扩展接口说明，但不要写成所有前台功能都直接调用存储过程。不要写 SQLite 已完成、JavaFX 26 已使用、AI Key 入库、全网全盘搜索已经完成、语音搜索已经完成。

隐私和后台信息边界：

正式报告中不要出现“群聊原话、私聊、AI 提示词、截图来源、参考同学作业、让模型生成、提示词”等过程性表述。可以把这些转化为中性表述，例如“根据课程设计要求”“在需求修订中补充”“系统设计时增加”。不要暴露聊天记录来源。

输出要求：

1. 先给出完整报告正文，使用适合粘贴到 Word 的 Markdown 标题结构。
2. 章节组织可以参考教师模板，但你可以自行优化，不要被当前草稿固定住。
3. 在报告中给出图表放置建议，例如“图 X 系统功能结构图，建议使用文件 xxx.png”，但不要虚构不存在的截图。
4. 使用表格呈现数据字典、核心表、索引/视图/触发器/存储过程、测试项等内容。
5. 最后附一个“提交前检查表”，逐项对应教师要求和评分点。
6. 如果某些材料之间冲突，请优先采用 README、schema.sql、关键 Java 代码和验证日志能证明的真实实现，并在报告中使用克制表述。

请直接开始写最终报告，不要先问我是否确认。
