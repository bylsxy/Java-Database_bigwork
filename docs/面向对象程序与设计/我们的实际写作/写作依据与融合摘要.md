# 写作依据与融合摘要

本文件保留本次修订的关键依据，方便后续检查 DOCX 时知道改动从哪里来。

## 1. 融合毕振岚单独稿的内容

- 补入“目录扫描 -> 查询或创建目录记录 -> 扫描磁盘图片 -> 数据库同步 -> 缩略图显示”的流程表达。
- 强化 `ImageViewerController`、`SlideshowController`、`ImageEditorController` 的职责划分，不再把所有界面工作都写成主窗口完成。
- 增加 `ThemeUtil` 全局主题背景的说明，体现设置页不只是普通配置表单。
- 个人总结改写时保留“代码实现压力最大、Controller 需要继续拆分”的真实口径。
- 删除单独稿中多出的第四名成员“黄奕恒”，第七组最终按毕振岚、陈厚华、徐阳三人写。

## 2. 参考其他组作业的可取点

- 第02组、04组、08组的思维导图报告目录层级较细，适合参考“系统设计拆小节”的结构，但正文不照搬。
- 第03组、06组、10组、12组通讯录报告的测试表更明确，吸收“输入/步骤、预期结果、实际结果、结论”的写法。
- 第09组同为电子图片管理程序，问题描述比较接近日常使用场景，吸收“先讲用户整理图片的痛点，再讲课程设计覆盖点”的顺序。
- 学委整理说明指出不少组的评分表不是 PDF、目录嵌套、临时文件未清理；本组附加说明中因此保留了交付文件清单和 JAR 启动排查。

## 3. 分工修订依据

- Git 中 `TreeRabbit15 <w183185720831@outlook.com>` 为毕振岚，主要提交 `20260518版本`、`20260519版本`、`20260519最终版`，对应代码实现和最终修复。
- `Xu Yang <1832652154@qq.com>` 主要提交早期工程、数据库/课程文档、作业收齐整理和本次 DOCX 生成脚本。
- 群聊中毕振岚安排陈厚华尝试音乐扩展、外部 AI、网盘方向；最终报告按“音乐扩展与外部接口调研、运行流程协助测试”写，不把未稳定落地的外部接口写成核心完成项。
- 评分表工作量按用户要求尽量平衡，写为 33.4%、33.3%、33.3%；正文分工则按实际证据写。

## 4. JAR 双击无反应说明

- 命令行启动日志显示目标 JAR 能启动：HikariCP 连接池启动成功，主界面初始化完成，应用程序启动成功。
- 双击无反应常见原因是 Windows 用 `javaw.exe` 启动，错误不会显示；也可能是 `.jar` 文件关联到了旧 Java、缺少 PostgreSQL 数据库，或没有带 `--enable-preview`。
- 附加说明中已经加入推荐启动命令：`java --enable-preview -jar 面向对象程序设计实践目标代码.JAR`。

